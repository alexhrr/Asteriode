
package asteroids.modelos;

import Memento.Caretaker;
import Memento.Originator;
import Memento.Partida;


public class AsteroidsModelos {

    public static void main(String[] args) {
        Ventana V = new Ventana();
        
        
        
        /*llamar al memento
        
        Partida partida= new Partida();
        partida.setPuntaje(5000);
        partida.setCoins(400);
        partida.setNick("Alex");
        Caretaker ct = new Caretaker();
        Originator or = new Originator();
        
        or.setEstado(partida);
        ct.addMemento(or.Guardar());
        
        
        partida = new Partida();
        partida.setPuntaje(10000);
        partida.setCoins(2000);
        partida.setNick("Chautik");
        or.setEstado(partida);
        ct.addMemento(or.Guardar());
        
        or.Restaurar(ct.getMemento(0));  */
        
    }
    
}
